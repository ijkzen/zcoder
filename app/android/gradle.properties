org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# 构建提速：子项目任务并行、复用任务输出缓存
org.gradle.parallel=true
org.gradle.caching=true
# configuration-cache 实测不兼容（2026-08-16）：flutter_foreground_task / flutter_local_notifications /
# sqflite_android / mobile_scanner 的 build.gradle 在执行阶段引用 ConfigurationContainer/DependencyHandler，
# 6 个 CC problem 导致构建失败。等插件迁移到 built-in Kotlin 后可再试。
